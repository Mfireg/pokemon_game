<h1>Create a new Character</h1>

  <% if @character.errors.any? %>
    <div>
      <h2><%= pluralize(@character.errors.count, "error") %> prevented this record from being saved:</h2>

      <ul>
        <% @character.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

<%= render 'form' %>